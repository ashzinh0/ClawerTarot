import { StyleSheet } from "react-native";

export const backgrounds = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#F0D6E6",
    alignItems: "center",
    justifyContent: "center",
  },
});

export const letters = StyleSheet.create({
  container: {
    justifyContent: "center",
  },
});
